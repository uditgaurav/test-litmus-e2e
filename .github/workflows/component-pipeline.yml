---
name: Build
on:
  push:
    branches: [master]
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'     
        required: true
        default: 'warning'
    
jobs:
  component-pipeline:
    container: litmuschaos/litmus-e2e:ci
    runs-on: self-hosted
    steps:
      - name: Getting the nodes
        run: |
          ls
          pwd
          whoami
          kubectl get nodes
          go --version

#   component-pipeline-2:
#     runs-on: self-hosted
#     steps:
#       - name: Getting the nodes
#         run: |
#           ls
#           pwd
#           whoami
#           kubectl get nodes
#           sleep 50
